doctype html
html
    head
        title My web
        link(rel='stylesheet' type='text/css' href='style.css')
    body
        #nav
        ul.nav-menu
            li.menu-item.brand My contact
            li.menu-item
                a(href='/') Home
            li.menu-item
                a(href='#') Users
            li.menu-item
                a(href='/create-user') Create User
        #content
        h1(style='text-align:center') Create User
        form#box-create(action="/create-user",method="POST")
            .area-row
                p.label Name
                input.input-label(type='text', name="name")
            .area-row
                p.label Phone
                input.input-label(type='text', name="phone")
            button.btn-sub(type='submit') Submit
